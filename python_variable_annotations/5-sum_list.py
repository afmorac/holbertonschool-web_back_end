#!/usr/bin/env python3
"""Este módulo define una función que suma una lista de floats."""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Devuelve la suma de una lista de números float."""
    return sum(input_list)
